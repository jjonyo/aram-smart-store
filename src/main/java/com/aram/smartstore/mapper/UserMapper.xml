<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aram.smartstore.mapper.UserMapper">

  <insert id="insert" parameterType="UserEntity" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO user (username, password, name, address, phone_number, state, creator_id,
                      modifier_id, created_at, modified_at)
    VALUES (#{username}, #{password}, #{name}, #{address}, #{phoneNumber}, #{state}, #{creatorId},
            #{modifierId}, NOW(), NOW())
  </insert>

  <select id="findByUsername" parameterType="String" resultType="UserEntity">
    SELECT *
    FROM user
    WHERE username = #{username}
  </select>

  <select id="findById" parameterType="Long" resultType="UserEntity">
    SELECT *
    FROM user
    WHERE id = #{id}
  </select>
  
</mapper>
