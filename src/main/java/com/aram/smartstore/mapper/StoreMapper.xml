<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aram.smartstore.mapper.StoreMapper">

  <insert id="insert" parameterType="StoreEntity" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO store (name, description, state, creator_id, modifier_id, created_at, modified_at)
    VALUES (#{name}, #{description}, #{state}, #{creatorId}, #{modifierId}, NOW(), NOW())
  </insert>
  
</mapper>
