<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aram.smartstore.mapper.StoreMemberMapper">

  <insert id="insert" parameterType="StoreMemberEntity" useGeneratedKeys="true"
    keyProperty="id">
    INSERT INTO store_member (store_id, user_id, type, creator_id, modifier_id, created_at,
                              modified_at)
    VALUES (#{storeId}, #{userId}, #{type}, #{creatorId}, #{modifierId}, NOW(), NOW())
  </insert>

</mapper>
